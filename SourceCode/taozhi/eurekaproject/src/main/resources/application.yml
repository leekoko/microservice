server:
  port: 8761

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:${server.port}/eureka/ #eureke registered address
    register-with-eureka: false
    fetch-registry: false

spring:
  application:
    name: eureka-project